# Copy to scripts/00-env and edit values. This file must never contain secrets.
# The parser accepts only the documented KEY=VALUE assignments; it does not execute shell code.

DC_IP="10.20.30.10"
DC_FQDN="dc1.ad.example.test"
DC_HOST="dc1"

REALM="AD.EXAMPLE.TEST"
DOMAIN="LAB"
DNS_FORWARDER="9.9.9.9"
LAN_CIDR="10.20.30.0/24"

# Optional sample directory objects created by 60-create-objects.sh.
OU_USERS_NAME="Lab Users"
OU_WORKSTATIONS_NAME="Lab Workstations"
NEW_USER="lab.user"
NEW_USER_GIVEN="Lab"
NEW_USER_SURNAME="User"

# Online domain backups contain secrets. Keep this path outside the repository.
BACKUP_DIR="/var/backups/samba-ad-dc"
